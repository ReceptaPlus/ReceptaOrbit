import { EmBreve } from "@/components/em-breve";

export default function VendasPage() {
  return (
    <EmBreve
      titulo="Vendas"
      descricao="A análise de vendas por IA é feita fora da plataforma. Em breve os resultados aparecerão aqui."
    />
  );
}
